function ConstructorItemList(props: { showItemDetails: (itemId: number) => void, backToMenu: () => void }) {
    const showItemDetails = props.showItemDetails
    const backToMenu = props.backToMenu

    return (
        <>
            
        </>
    );
}

export default ConstructorItemList;